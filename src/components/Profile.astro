<section class="profile">
  <p class="name">장명근</p>
  <div class="links">
    <a href="https://github.com/CDDing">GitHub</a>
    <a href="https://www.linkedin.com/in/%EB%AA%85%EA%B7%BC-%EC%9E%A5-583bab26a/">LinkedIn</a>
  </div>
</section>

<style>
  /* 아바타를 두지 않는다 (스펙 §4). 이미지 자산이 없어 빈 원만 남기 때문. */
  .profile {
    padding: 32px 0 22px;
    border-bottom: 1px solid var(--divider);
  }
  .name {
    margin: 0;
    color: var(--heading);
    font-size: 1rem;
    font-weight: 700;
    letter-spacing: -0.015em;
  }
  .links {
    display: flex;
    gap: 8px;
    margin-top: 12px;
  }
  .links a {
    border: 1px solid var(--border);
    border-radius: 14px;
    color: var(--text-muted);
    font-size: 0.75rem;
    padding: 3px 11px;
  }
  .links a:hover {
    border-color: var(--accent-border);
    color: var(--accent-strong);
  }
</style>
