<section class="profile">
  <p class="name">장명근</p>
  <p class="role">Michelo Robotics</p>
  <p class="bio">
    GPU와 그래픽스를 파고듭니다. 명세를 번역하고, 막히면 물어보고, 남는 걸 여기 적습니다.
  </p>
  <div class="links">
    <a href="https://github.com/CDDing">GitHub</a>
    <a href="https://www.linkedin.com/in/%EB%AA%85%EA%B7%BC-%EC%9E%A5-583bab26a/">LinkedIn</a>
  </div>
</section>

<style>
  /* 아바타를 두지 않는다 (스펙 §4). 이미지 자산이 없어 빈 원만 남기 때문. */
  .profile {
    padding: 32px 0 22px;
    border-bottom: 1px solid var(--divider);
  }
  .name {
    margin: 0;
    color: var(--heading);
    font-size: 1rem;
    font-weight: 700;
    letter-spacing: -0.015em;
  }
  .role {
    margin: 2px 0 0;
    color: var(--text-muted);
    font-size: 0.8rem;
  }
  .bio {
    margin: 8px 0 0;
    font-size: 0.88rem;
    line-height: 1.65;
    max-width: 52ch;
  }
  .links {
    display: flex;
    gap: 8px;
    margin-top: 10px;
  }
  .links a {
    border: 1px solid var(--border);
    border-radius: 14px;
    color: var(--text-muted);
    font-size: 0.75rem;
    padding: 3px 11px;
  }
  .links a:hover {
    border-color: var(--accent-border);
    color: var(--accent-strong);
  }
</style>
